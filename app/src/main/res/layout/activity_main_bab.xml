<?xml version="1.0" encoding="utf-8"?>
<androidx.coordinatorlayout.widget.CoordinatorLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <com.google.android.material.appbar.AppBarLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content">

        <com.google.android.material.appbar.MaterialToolbar
            android:id="@+id/topAppBarBab"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:title="@string/title_main_bab"
            android:background="@color/nicestart_primary"
            android:titleTextColor="@color/nicestart_on_primary"
            app:titleCentered="true"
            app:navigationIcon="@drawable/arrow_back"
            app:navigationContentDescription="@string/back"
            app:titleTextAppearance="@style/TextAppearance.Material3.TitleMedium" />
    </com.google.android.material.appbar.AppBarLayout>

    <!-- Contenido provisional (luego lo cambiamos por lo que haga Bab) -->
    <TextView
        android:id="@+id/tvBab"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:gravity="center"
        android:text="@string/main_bab_ok"
        android:textSize="20sp"
        app:layout_behavior="@string/appbar_scrolling_view_behavior"/>

</androidx.coordinatorlayout.widget.CoordinatorLayout>
